# Copy to .env (already gitignored) and fill in your own keys.
# Together: api.together.ai/settings/api-keys
# Exa:      dashboard.exa.ai/api-keys
#
# Usage: set -a && source .env && set +a && mix phx.server

TOGETHER_API_KEY=
EXA_API_KEY=
